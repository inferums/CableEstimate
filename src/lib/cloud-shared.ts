/*
 * Общее для браузера и серверной функции лежит рядом с самой функцией —
 * в api/_shared.ts.
 *
 * Причина не в красоте: сборщик функций на Vercel собирает только то, что
 * лежит в каталоге api, и статический импорт из src разваливал функцию на
 * старте — наружу уходил FUNCTION_INVOCATION_FAILED без объяснений. Имя
 * файла начинается с подчёркивания, поэтому отдельной функцией он не
 * становится.
 *
 * Здесь остаётся только переизлучение, чтобы браузерный код по-прежнему
 * обращался к своему слою, а не лазил в api напрямую.
 */
export * from "../../api/_shared";
